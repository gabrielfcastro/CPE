#include <iostream>
using namespace std;

int main(){
    // Declara as variáveis 
    int x,y,z;

    // Resolvendo as operações matemáticas
    x = 7 + 3 * 6/2 -1;
    y = 2 * abs(2) + 2 * 2/2;
    z = (3 * 9 * (3 + (9 * 3/3 ) ) );

    // Imprimindo o resultado no Terminal
    cout << x << ' ' << y << ' ' << z << endl;
    return 0;

}

